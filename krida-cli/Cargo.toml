[package]
name = "krida-cli"
version.workspace = true
edition.workspace = true


[dependencies]
krida-core = { path = "../krida-core" }
krida-strategy = { path = "../krida-strategy" }
serde = { version = "1.0.228", features = ["derive"] }
toml = "1.1.0"
